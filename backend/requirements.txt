fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.12
pydantic==2.10.3
pydantic-settings==2.6.1
sqlalchemy==2.0.36
alembic==1.14.0
redis==5.2.0
rq==1.16.2
orjson==3.10.12
httpx==0.27.2

# Supabase (DB + Storage persistence)
supabase==2.10.0

# Media / analysis
numpy==1.26.4
opencv-python==4.10.0.84
mediapipe==0.10.18
faster-whisper==1.1.0
librosa==0.10.2.post1
soundfile==0.12.1
praat-parselmouth==0.4.5

# Vision upgrades
# InsightFace is optional. On Linux (e.g. Railway slim images) pip often tries to build from source
# and fails due to missing build deps / no wheels. The app gracefully falls back when it's absent.
insightface==0.7.3; platform_system == "Windows"
# CPU build avoids CUDA DLL errors on machines without a working NVIDIA stack.
# For GPU: pip uninstall onnxruntime && pip install onnxruntime-gpu==1.19.2 and set VISION_INSIGHTFACE_GPU=true
onnxruntime==1.19.2
scenedetect[opencv]==0.6.4
# YouTube ingest
yt-dlp==2025.1.26

